# Copy to .env.local and adjust if needed (Next.js loads .env.local automatically).
DATABASE_URL=postgres://postgres:dev@127.0.0.1:5432/webhook_playground
# Optional: only if request headers do not match your public URL (custom domain, etc.)
# NEXT_PUBLIC_APP_URL=https://your-app.onrender.com
