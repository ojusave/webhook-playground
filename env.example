# Local development only — copy to `.env.local`. Production uses DATABASE_URL from render.yaml (Render Postgres).
# Use any Postgres you have (local install, tunnel, etc.):
# DATABASE_URL=postgres://USER:PASSWORD@HOST:5432/webhook_playground
#
# Optional:
# NEXT_PUBLIC_APP_URL=https://your-custom-domain.com
